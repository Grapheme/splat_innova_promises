
    <!-- Don't forget add to parent page to scripts section JS code for correct functionality, some like this: -->
    <!-- loadScript("/js/modules/gallery.js"); -->


	<div class="egg-dropzone dropzone" data-name="<?php echo $name; ?>" data-gallery_id="<?php echo (isset($gallery) && is_object($gallery)) ? (int)$gallery->id : '0'; ?>"<? if (isset($params) && is_array($params) && count($params)) { foreach($params as $key => $value) { echo ' data-' . $key . '="' . $value . '"'; } } ?>></div>
    <div class="superbox_" style="margin-top:10px;">

    	<input type="hidden" name="<?php echo $name; ?>[gallery_id]" value="<?php echo (isset($gallery) && is_object($gallery)) ? (int)$gallery->id : '0'; ?>" />

        <?php if(@is_object($gallery)): ?>
            <? $gallery_photos = $gallery->photos()->get(); ?>
            <?php if($gallery_photos): ?>

            <div class="photo-previews sortable">
            	<?php foreach($gallery_photos as $photo): ?>
            	<div class="photo-preview sortable_item" data-id="<?php echo $photo->id; ?>" style="background-image:url(<?php echo URL::to($photo->thumb()); ?>);">
            		<a href="<?php echo URL::to($photo->path()); ?>" target="_blank" title="Полноразмерное изображение" style="display:block; height:100%; color:#090; background:transparent"></a>
            		<a href="#" class="photo-delete" data-photo-id="<?php echo $photo->id; ?>" style="">Удалить</a>
            	</div>
            	<?php endforeach; ?>
            </div>

            <?php endif; ?>
        <?php endif; ?>
    </div>
    <div class="clear"></div>
