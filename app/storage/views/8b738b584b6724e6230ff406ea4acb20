
    <!-- Don't forget add to parent page to scripts section JS code for correct functionality, some like this: -->
    <!-- loadScript("/js/modules/gallery.js"); -->

    <?
    $photo_exists = @is_object($photo) && $photo->id;
    #Helper::dd($photo_exists);
    ?>
    <?php if($photo_exists): ?>
    	<input type="text" name="<?php echo $name; ?>" value="<?php echo $photo->id; ?>" class="uploaded_image_<?php echo $photo->id; ?> uploaded_image_cap" style="position:absolute; left:-10000px;" />
    <?php else: ?>
        <input type="text" name="<?php echo $name; ?>" value="" class="uploaded_image_false uploaded_image_cap" style="position:absolute; left:-10000px;" />
    <?php endif; ?>
    <div>
    	<div class="egg-dropzone-single dropzone" data-name="<?php echo $name; ?>" data-gallery_id="0"<? if (isset($params) && is_array($params) && count($params)) { foreach($params as $key => $value) { echo ' data-' . $key . '="' . $value . '"'; } } ?><? if ($photo_exists) { echo " style='display:none'";} ?>></div>
        <div class="superbox_ photo-preview-container" style="margin-top:10px;">

            <?php if($photo_exists): ?>

            	<div class="photo-preview photo-preview-single" style="background-image:url(<?php echo URL::to($photo->thumb()); ?>);">
            		<a href="<?php echo URL::to($photo->path()); ?>" target="_blank" title="Полноразмерное изображение" style="display:block; height:100%; color:#090; background:transparent"></a>
            		<a href="#" class="photo-delete-single" data-photo-id="<?php echo $photo->id; ?>" style="">Удалить</a>
            	</div>

            <?php else: ?>

            	<div class="photo-preview photo-preview-single" style="display:none;">
            		<a href="#photo-path" target="_blank" title="Полноразмерное изображение" class="photo-full-link" style="display:block; height:100%; color:#090; background:transparent"></a>
            		<a href="#" class="photo-delete-single" data-photo-id="#photo-id" style="">Удалить</a>
            	</div>

            <?php endif; ?>

        </div>
    </div>