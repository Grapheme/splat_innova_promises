
    <ul class="index-news">
    	<?php foreach($news as $new): ?>
        <li>
            <div class="news-date"><?php echo Helper::rdate("j M Y", strtotime($new->published_at)); ?></div>
            <a href="<?php echo URL::route('news'); ?>#<?php echo $new->slug; ?>" class="us-link">
                <?php echo $new->meta->title; ?>

            </a>
    	<?php endforeach; ?>
    </ul>
